# staticcheck 配置。
# 关掉 SA6002:该检查对「向 sync.Pool 存放非指针值(尤其 []byte 切片)会额外分配」发告警,
# 但对 []byte 缓冲池而言这是刻意且惯用的写法,业界普遍视为噪声。其余检查(含 U1000 未用代码、
# SA1019 deprecated、SA4xxx 正确性)全部保留。
checks = ["inherit", "-SA6002"]
